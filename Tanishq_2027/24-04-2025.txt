class Solution {
    public int fib(int n) {
        int fir = 0, sec = 1;
        int idx = 0;
        while(idx<n){
            int third = fir+sec;
            fir = sec;
            sec = third;
            idx++;
        }
        return fir;
    }
}
